<%= form_for @changeset, game_path(@conn, :update, @game), fn f -> %>
  <div class="form-group">
    <%= text_input f, :title, placeholder: "Title", class: "form-control" %>
    <%= error_tag f, :title %>
  </div>

  <%= submit "Save Game", class: "btn btn-primary" %>
<% end %>